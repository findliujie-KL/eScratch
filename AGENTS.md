# Development workflow

- After every feature update, rebuild and run the latest WPF development build so the user can test it immediately. Restart the existing WPF development instance when necessary, preserving its saved draft and settings. Do not create an installer or portable release unless requested.
